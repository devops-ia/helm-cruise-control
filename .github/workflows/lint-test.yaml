name: Lint and test PR Cruise Control

on:
  workflow_dispatch:
  pull_request:

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          fetch-depth: 0

      # default install latest (stable)
      - name: Set up Helm
        uses: azure/setup-helm@1a275c3b69536ee54be43f2070a358922e12c8d4 # v4.3.1

      - uses: actions/setup-python@83679a892e2d95755f2dac6acb0bfd1e9ac5d548 # v6.1.0
        with:
          python-version: 3.x
          check-latest: true

      - name: Set up chart-testing
        uses: helm/chart-testing-action@6ec842c01de15ebb84c8627d2744a0c2f2755c9f # v2.8.0
        with:
          yamale_version: "6.0.0"

      - name: Run chart-testing (list-changed)
        id: list-changed
        run: |
          changed=$(ct list-changed --config .github/ct.yaml)
          if [[ -n "${changed}" ]]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run chart-testing (lint)
        if: steps.list-changed.outputs.changed == 'true'
        run: ct lint --config .github/ct.yaml

      # # Deploy Kafka KRaft cluster for testing
      # - name: Run Kafka KRaft Broker
      #   if: steps.list-changed.outputs.changed == 'true'
      #   uses: spicyparrot/kafka-kraft-action@f36354d3fe55174a962d4fc544f69c849dca29a0 # v1.1.3
      #   with:
      #     kafka-version: "3.7.0"

      # # Verify Kafka cluster is running
      # - name: Verify Kafka connectivity
      #   if: steps.list-changed.outputs.changed == 'true'
      #   run: |
      #     echo "Testing connection to Kafka at ${{ env.kafka_runner_address }}:9093"
      #     timeout 30 bash -c 'until nc -z ${{ env.kafka_runner_address }} 9093; do echo "Waiting for Kafka..."; sleep 2; done'
      #     echo "Kafka is ready!"

      # # kafka_runner_address env var created by the kafka-kraft-action step
      # - name: Update bootstrap.servers in ci-common-values.yaml
      #   if: steps.list-changed.outputs.changed == 'true'
      #   run: |
      #     KAFKA_BOOTSTRAP="${{ env.kafka_runner_address }}:9093"
      #     echo "Kafka bootstrap server: ${KAFKA_BOOTSTRAP}"
      #     sed -i "s|bootstrap.servers:.*|bootstrap.servers: \"${KAFKA_BOOTSTRAP}\"|" charts/cruise-control/ci/ci-common-values.yaml
      #     echo "Updated ci-common-values.yaml:"
      #     grep "bootstrap.servers" charts/cruise-control/ci/ci-common-values.yaml

      # - name: Create kind cluster
      #   if: steps.list-changed.outputs.changed == 'true'
      #   uses: helm/kind-action@92086f6be054225fa813e0a4b13787fc9088faab # v1.13.0

      # - name: Run chart-testing (install)
      #   if: steps.list-changed.outputs.changed == 'true'
      #   run: ct install --config .github/ct.yaml
